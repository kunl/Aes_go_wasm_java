# aes_go_wasm_java
aes for go and java; build go fo wasm and use wasm parse java response.


vscode setting config

settings.json
``` json
{
    "go.toolsEnvVars": {
        "GOOS": "js",
        "GOARCH": "wasm"
    }
}
```
